<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user12">

	<!-- 아이디 중복 검사를 위한 SQL -->
	<select id="idcheck" resultType="java.lang.String"
		parameterType="java.lang.String">
		select email from springuser where email = #{email}
	</select>
	
	<!-- 회원가입을 위한 sql 작성  -->
	<insert id ="register" 
	parameterType="User12">
	insert into springuser(email, pw, nickname, image)
	values(#{email}, #{pw},#{nickname},#{image})
	</insert>
	
	<!-- 로그인 처리를 위한 sql 작성 -->
	<!-- 입력 받는 것: email    돌려주는 것: email, pw, nickname, image -->
	<select id="login" resultType="User12"
		parameterType="java.lang.String">
		select email, pw, nickname, image from springuser where email = #{email}
	</select>




</mapper>